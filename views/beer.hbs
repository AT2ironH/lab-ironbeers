<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beers</title>
</head>
<body>
    <div class="container">
    {{#each beersFromApi}}
        <img class="randomImg" src={{this.image_url}}>
        <div class="beerText">
            <h1 class="titleSmall">Name:{{this.name}}</h1>
            <h3>{{this.tagline}}</h3>
            <p>{{this.description}}</p>
        
        </div>
    {{/each}}
    </div>
</body>
</html>